%h1 Creating coast

= error_messages_for :coast

- form_for(:coast, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
